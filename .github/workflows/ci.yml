name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  setup:
    uses: ./.github/workflows/setup.yml

  lint:
    needs: setup
    steps:
    - name: Run lint
      run: yarn eslint .

  typecheck:
    needs: setup
    steps:
    - name: Run typecheck
      run: yarn nuxi typecheck
